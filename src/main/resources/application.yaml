server.port: 8085
playwright.headless: false

spring:
  cache.type: redis
  data:
    redis:
      host: 192.168.1.222
      port: 6379
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
