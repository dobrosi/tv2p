spring:
  application.name: tv2p
  jmx.unique-names: true
  security:
    user:
      name: admin
      password: secret123
      roles: ADMIN
  config.import: configserver:http://sba:sba12358@192.168.1.222:8888/config
  boot.admin.client:
    url: http://192.168.1.222:8888/admin
    instance:
      metadata:
        user:
          name: ${spring.security.user.name}
          password: ${spring.security.user.password}
security:
  jmx:
    enabled: true
management:
  endpoints:
    jmx.domain: com.github.dobrosi
    web.exposure.include: '*'
  endpoint:
    health.show-details: always
    jolokia.enabled: true
    env.show-values: ALWAYS
